# Audit Ready

This branch is the clean audit line from current `main`. It contains only CI/documentation consolidation and intentionally does not replay historical migrations already superseded on `main`.
